# Backward-compatible no-op override.
#
# GHCR image deployment is now the default in docker-compose.yml. Older docs used:
#   docker compose -f docker-compose.yml -f docker-compose.ghcr.yml up -d --no-build
# Keeping this file avoids breaking that command; it intentionally does not override services.

services: {}
